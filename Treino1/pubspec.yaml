name: treino1
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: '>=3.0.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  shared_preferences: ^2.0.12
  math_expressions: ^2.2.0
  cupertino_icons: ^1.0.2
  flutter_simple_calculator: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
